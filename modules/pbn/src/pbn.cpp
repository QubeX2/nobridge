#include "pbn.h"

#include "parser.h"
namespace nobridge::pbn {

    PbnPtr process(GameList& glist) {
        for (auto tlist : glist) {
        }
        return nullptr;
    }

}  // namespace nobridge::pbn
